+ Use FreeRDP master branch.
+ Go 1.5.1

### build
+ Need golang.org/x/net/websocket
+ FreeRDP dynamic library
There is a example in build.sh

### Run

	$./webRDP

Enter localhost:8080 in  web browser.

